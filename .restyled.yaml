also_exclude:
  - "*/dist/**/*"
  - "*/src/__snapshots__/**/*"
  - "*/test-files/**/*"

restylers:
  # Disable other json formatters (using prettier-json)
  - "!clang-format"
  - "!jq"

  # This is a bug in the manifest, I'd say
  - prettier:
      include:
        - "run/src/**/*.ts"

  - "*"
